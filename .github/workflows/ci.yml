name: Sample Energy OS CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Automated Pytest Suite
      run: |
        PYTHONPATH=. pytest tests/test_suite.py -v

    - name: Execute Full Site Pipeline & Audit Export
      run: |
        PYTHONPATH=. python -m src.sample_energy_os --config site_config.json --run-sensitivity --export-audit

    - name: Verify Audit Ledger Artifact
      run: |
        test -f data/AUDIT_LEDGER.json
        test -f data/sensitivity_matrix_results.csv
